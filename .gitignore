node_modules
.next
out
.env*
!.env.example
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
tsconfig.tsbuildinfo
data/reports

# JUST.DB エクスポートCSV（顧客PII＝顧客名・住所・担当者、または内部の売価/原価を含む）。
# ルート直下に置く作業用エクスポートは入れない。テスト fixture 等は tests/ 配下に置けば対象外。
/*.csv

# デバッグ用スクショ・出力PDF（動作確認でルート直下に落ちる一時物。物件名入りPDFもあり得る）。
# 意図的に残す資料は docs/ 配下へ。
/*.png
/*.pdf
