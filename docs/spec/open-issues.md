# 未確定事項一覧（v1.0）

解決済みは `architecture/`・`spec/requirements.md` に反映済み。以下は現に未確定なもの。

## 0. 【最優先・調査中】JUST.DB API 予算（5000回/日）の内訳

設計を確定できない唯一のブロッカー。`architecture/justdb-supabase-integration.md` §4 と対。

- **2026-06-17 22:00 時点で 4345/5000 を消費（残り約655）。正体不明の消費が約9割。** 別リポジトリで内訳を調査中。
- headroom が全レーン（マスタ同期・ケース取得・書き戻し）の実装方式を左右するため、判明まで確定しない。
- 明らかにすべき点:
  1. 単一消費者か、多数の合算か
  2. 定期ポーリングか、活動量に比例か
  3. 生きている連携か、惰性（未使用）か ← 最有力かつ最も削れる
  4. 読み/書きの別、枠は組織単位かAPIキー単位か
  5. 5000 は引き上げ可能なプランか
- 分岐: 削れる→ Lane A は pull増分のまま余裕／削れず必須→ マスタを データフローCSV（API 0）へ逃がし、報告あたりコールを最小化。

## 1. 連携・認証
- 社内SSO/OIDC を初期リリースに含めるか
- ~~署名トークン検証をいつ有効化するか~~ → **実装・有効化済**（`launch-token.ts`/`REPORT_LINK_SECRET`）。Slack写真報告フロー `/report/photo` では**必須**、`/report/new` は任意。※ローカルは初期プレースホルダ値だった経緯あり→本番は実乱数（2026-06-19）
- JUST.DB の読み取り/書き込みAPIの実アクセス（現状ミラーはシード代替）
- 薬剤資材→Supabase の実同期方式（pull増分 / メールCSV）。**§0 の予算調査で確定**
- 書き戻しランディングテーブルの実在/スキーマと、JUST.DB側データフローでの分解設定

## 2. 帳票レイアウト
- 紺谷V／写真報告書／**融合**の厳密レイアウト（座標・文字サイズ・印影/承認欄）
- フォントと行間の固定値

## 3. 写真・注記
- ~~**赤丸注記（タッチ描画）の実装仕様**~~ → **実装済（2026-06-19）**。透明SVG＋Pointer Events・正規化座標・UNDO/REDO（`src/components/photo-annotator.tsx`／正本アーキ §6）。
- 写真1件あたりの解像度・容量上限（画像前処理＝リサイズ/圧縮の置き場は Phase5・未着手）

## 4. 薬剤・在庫
- 薬剤資材テーブルに 害虫→薬剤、薬剤→処理方法 の関連が実在するか（無ければミラー側に保持）
- 現状、薬剤の廃番は物理削除か状態フラグか（増分同期の前提。方針は論理削除推奨＝`integration` §5）
- 在庫管理を在庫台帳（移動記録）として持つか、どの段階で着手するか

## 5. データ保管・書き戻し
- Drive のファイル名規則・重複時の扱い・保存失敗時のリトライ
- 書き戻しフィールドの確定（金額・回数・薬剤・要約）と要約の用途/精度
- 設定ファイルの互換ポリシー（バージョン差異時の移行）
- 保管期間と削除ポリシー

## 6. 運用・展開
- 監査ログ要件、障害時の再出力手順
- 実行基盤の確定（Vercel か他か）。ローカルFS非永続の前提整理
- 東京支店展開、フォーマット標準化の進め方（「統合の手前」運用）
- モバイル/老眼向けUI（現場入力）の着手時期
- 自前構築 vs 既製（build/buy）の最終判断
