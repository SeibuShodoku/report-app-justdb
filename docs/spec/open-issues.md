# 未確定事項一覧（v1.0）

解決済みは `architecture/`・`spec/requirements.md` に反映済み。以下は現に未確定なもの。

## 1. 連携・認証
- 社内SSO/OIDC を初期リリースに含めるか
- 署名トークン検証をいつ有効化するか
- JUST.DB の読み取り/書き込みAPIの実アクセス（現状ミラーはシード代替）
- 薬剤資材→Supabase の実同期（頻度・トリガー）。ケースデータはライブ取得かミラーか

## 2. 帳票レイアウト
- 紺谷V／写真報告書／**融合**の厳密レイアウト（座標・文字サイズ・印影/承認欄）
- フォントと行間の固定値

## 3. 写真・注記
- **赤丸注記（タッチ描画）の実装仕様**
- 写真1件あたりの解像度・容量上限

## 4. 薬剤・在庫
- 薬剤資材テーブルに 害虫→薬剤、薬剤→処理方法 の関連が実在するか（無ければミラー側に保持）
- 在庫管理を在庫台帳（移動記録）として持つか、どの段階で着手するか

## 5. データ保管・書き戻し
- Drive のファイル名規則・重複時の扱い・保存失敗時のリトライ
- 書き戻しフィールドの確定（金額・回数・薬剤・要約）と要約の用途/精度
- 設定ファイルの互換ポリシー（バージョン差異時の移行）
- 保管期間と削除ポリシー

## 6. 運用・展開
- 監査ログ要件、障害時の再出力手順
- 実行基盤の確定（Vercel か他か）。ローカルFS非永続の前提整理
- 東京支店展開、フォーマット標準化の進め方（「統合の手前」運用）
- モバイル/老眼向けUI（現場入力）の着手時期
- 自前構築 vs 既製（build/buy）の最終判断
