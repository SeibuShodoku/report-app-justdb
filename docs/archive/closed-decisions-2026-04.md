# クローズ済み論点（2026-04）※一部は後続で見直し

当時 `open-issues` から移動した決定の記録。**取り消し線/注記は2026-06時点の見直し**。
現行の正は `spec/requirements.md` と `architecture/` を参照。

## 1. JUST.DB連携

- JUST.DBは起動元とする（現行も維持）
- ~~書き戻しは行わない~~ → **見直し**: 限定フィールド（金額/回数/薬剤）＋要約をAPIで書き戻す
- `driveFolderUrl` を正規入力 / `driveFolderId` を代替入力（現行も維持）
- 当面トークンなし運用（現行も維持・将来検証）

## 2. 保存方式

- 既定は方式B（帳票に割り当てた写真のみ保存）（現行も維持）
- 方式A（全写真保存）はオプション（現行も維持）
- 追記（2026-06）: 保存先はGoogle Drive、薬剤資材等の参照はSupabaseミラー

## 3. 実行基盤

- Vercel採用（現行は**候補**。モックはローカル＋Supabaseで稼働。ローカルFS保存は本番非永続）
